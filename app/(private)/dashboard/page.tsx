/**
 * DASHBOARD ROOT PAGE
 *
 * Dalam pola "Composable Layouts" dengan Parallel Routes,
 * konten utama seringkali dipindahkan ke slots (seperti @overview).
 *
 * Halaman ini (`page.tsx`) dirender ke dalam prop `children` di layout.
 * Kita biarkan kosong atau minimal sebagai placeholder.
 */

export default function DashboardPage() {
  return null;
}

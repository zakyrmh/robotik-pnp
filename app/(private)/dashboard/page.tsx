/**
 * DASHBOARD ROOT PAGE
 *
 * This page acts as the default `children` slot.
 * Since we are using parallel routes (@overview, etc.), this can remains empty
 * or return null to avoid duplication.
 */

export default function DashboardPage() {
  return null;
}
